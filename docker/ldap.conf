# OpenLDAP client configuration.
# Mounted read-only by docker-compose.yml at /etc/ldap/ldap.conf.
# Place the AD root CA certificate (PEM) at ./ad-ca.crt on the Docker host.
TLS_CACERTFILE /run/secrets/ad-ca.crt
