# Global Apache server security settings.
# Placed in conf-enabled/ so these directives take effect at server level,
# before any VirtualHost is evaluated (required for ServerTokens and ServerSignature).

ServerTokens Prod
ServerSignature Off
TraceEnable Off
